# --- Generated artifacts ---
build/
dist/
logs/

# --- Cloned third-party ---
CanvOS/
# Palette-appliance CanvOS checkout (separate tree: the appliance build
# patches .arg/Dockerfile/user-data differently than kairos_build does).
CanvOS-appliance/

# --- Big binary images (anywhere) ---
*.iso
*.raw
*.raw.lz4
*.qcow2
*.img

# --- Logs + runtime state ---
*.log
*.pid
*.sock
*.retry

# --- OVMF/UEFI variables ---
ovmf-vars.fd
*.fd

# --- SSH keys generated during runs ---
bcm-kairos-key
bcm-kairos-key.pub
.bcm-ssh-config

# --- Ansible bits ---
*.retry
.ansible/

# --- Editor / OS ---
.vscode/
.idea/
.DS_Store
*.swp
*~

# --- Python ---
__pycache__/
*.pyc
.venv/
venv/

# --- Claude ---
.claude/
CLAUDE.md

# --- Checksums ---
*.sha256

# --- Secrets ---
.jfrog-curl.cfg
inventory/group_vars/all.yml
bcm-discovery-*.yml
bcm-bridge-inventory.yml
ursa-proxmox-access/

*.customer-backup

# --- Scratch / local artifacts (never commit) ---
*.mov
Untitled
profiles/kairos-dgx4.yml

# --- Palette appliance: licensed Spectro Cloud artifacts (never commit) ---
# The content bundle alone is ~10 GB and none of it is ours to redistribute.
artifacts/*
!artifacts/.gitkeep
!artifacts/README.md
*.tar.zst
*.sig.bin
*.sig.hex
spc.tgz

# --- Palette appliance: generated credentials + keys ---
.run-credentials.yml
*.pem
*.key
*.crt
!**/*.pub
